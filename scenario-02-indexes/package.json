{
  "name": "scenario-02-indexes",
  "version": "1.0.0",
  "description": "System Design Scenario 2: Indexes and Slow Queries",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "seed": "node db/seed.js",
    "loadtest": "node scripts/loadtest.js",
    "status": "node -e \"const {Pool} = require('pg'); const db = new Pool({host:'postgres',user:'postgres',password:'postgres',database:'shopdb'}); db.query('SELECT COUNT(*) FROM orders').then(r => { console.log('✅ Database ready. Orders in DB:', r.rows[0].count); db.end(); }).catch(() => { console.log('❌ Database not ready yet.'); db.end(); })\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "prom-client": "^15.1.0"
  },
  "devDependencies": {
    "autocannon": "^7.14.0",
    "nodemon": "^3.0.0"
  }
}
